{{ if .Values.htpasswdAuth.enabled -}}
{{- range $dest := .Values.htpasswdAuth.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: htpasswd-auth{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ $.Values.htpasswdAuth.git.repoURL }}
    targetRevision: {{ $.Values.htpasswdAuth.git.targetRevision }}
    path: {{ $.Values.htpasswdAuth.git.path }}
    helm:
      values: |
        adminPassword: {{ $.Values.htpasswdAuth.adminPassword | quote }}
  destination:
    name: {{ $dest }}
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}