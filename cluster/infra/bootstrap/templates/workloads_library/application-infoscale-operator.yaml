{{ if .Values.infoscaleOperator.enabled -}}
{{- range $dest := .Values.infoscaleOperator.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infoscale-operator{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ $.Values.infoscaleOperator.git.repoURL }}
    targetRevision: {{ $.Values.infoscaleOperator.git.targetRevision }}
    path: {{ $.Values.infoscaleOperator.git.path }}
  destination:
    name: {{ $dest }}
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
