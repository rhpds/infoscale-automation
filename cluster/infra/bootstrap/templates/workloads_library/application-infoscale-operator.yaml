{{ if .Values.infoscaleOperator.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infoscale-operator
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ .Values.infoscaleOperator.git.repoURL }}
    targetRevision: {{ .Values.infoscaleOperator.git.targetRevision }}
    path: {{ .Values.infoscaleOperator.git.path }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
