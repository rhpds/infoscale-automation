{{ if .Values.example1InlineResource.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: example1-inline-resource
  namespace: {{ .Values.example1InlineResource.namespace }}
  labels:
    app: example1-inline-resource
    tenant: {{ .Values.tenant.name }}
spec:
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: example1-inline-resource
  type: ClusterIP
{{- end }}
