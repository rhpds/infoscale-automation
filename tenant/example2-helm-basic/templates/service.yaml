apiVersion: v1
kind: Service
metadata:
  name: example2-helm-basic
  namespace: {{ .Values.example2HelmBasic.namespace }}
  labels:
    app: example2-helm-basic
    tenant: {{ .Values.tenant.name }}
spec:
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: example2-helm-basic
  type: ClusterIP
